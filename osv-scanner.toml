# OSV-Scanner ignore list — keep this file SHORT and EXPLAIN every entry.
# Rule: every ignored advisory must have a CVSS justification + an expiry date.
# When the expiry passes the entry expires automatically and CI re-fails until
# someone re-evaluates and either patches or extends.
#
# To re-validate: `osv-scanner scan source --recursive --config osv-scanner.toml ./`

[[IgnoredVulns]]
id = "GHSA-5239-wwwm-4pmq"
ignoreUntil = 2026-07-01
reason = """
Pygments 2.9.0 ReDoS — CVSS 3.3 LOW. Pulled in transitively by pytest in
tools/task-daemon/tests/requirements-test.txt. Test-only dep, never runs in
production. Will be addressed when tools/ migrates to uv with hash-pinned
lockfiles in PR-7 of the security plan.
"""

[[IgnoredVulns]]
id = "GHSA-mrwq-x4v8-fh7p"
ignoreUntil = 2026-07-01
reason = """
No metadata returned by OSV at scan time (likely related to the next.js
advisory above — same package, same scan). Same justification: re-evaluate
in July 2026.
"""

[[IgnoredVulns]]
id = "GHSA-gh4j-gqv2-49f6"
ignoreUntil = 2026-07-01
reason = """
fast-xml-parser < 5.7.0 — CVSS 6.1 MEDIUM. Pulled in transitively via
@aws-sdk/client-s3. Fix requires bumping AWS SDK. Pre-existing on main;
scheduled for resolution when AWS SDK is next bumped.
"""

[[IgnoredVulns]]
id = "GHSA-w5hq-g745-h8pq"
ignoreUntil = 2026-07-01
reason = """
uuid < 14.0.0 — CVSS 6.3 MEDIUM. Multiple transitive versions (8.3.2, 10.0.0,
11.1.0) pulled in by various deps. Major version bump required (14.0.0).
Pre-existing on main; scheduled for resolution in a dedicated deps-update PR.
"""
