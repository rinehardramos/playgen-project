shamefully-hoist=true

# --- supply-chain hardening ---
# Reject packages published less than 72h ago to blunt zero-day malicious publishes.
minimum-release-age=4320
# Fail installs on advisories at moderate or higher (CI runs `pnpm audit --audit-level=high` separately).
audit-level=high
fund=false
engine-strict=true
