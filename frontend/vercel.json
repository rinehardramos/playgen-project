{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "rm -rf .next && npm run build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": false
    }
  }
}
