{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "echo '=== src/lib ===' && ls src/lib/ && rm -rf .next && npm run build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": false
    }
  }
}
