[build]
builder = "DOCKERFILE"
dockerfilePath = "services/playlist/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
