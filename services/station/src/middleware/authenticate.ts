// This local middleware is superseded by @playgen/middleware.
// All routes import authenticate/requirePermission from @playgen/middleware directly.
// Kept for reference only.

export { authenticate, requirePermission, requireStationAccess, requireCompanyMatch } from '@playgen/middleware';
